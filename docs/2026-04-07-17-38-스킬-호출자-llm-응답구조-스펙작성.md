# 작업 기록 — 스킬 호출자 LLM 응답 구조 스펙 작성

- 시각: 2026-04-07 17:38 (Asia/Seoul)
- 사용자 프롬프트:
  - "근데 원래 내가 원했던건 스킬로 호출할때는 LLM을 스킬을 호출하는 쪽이 담당했으면 좋겠는데.. 그러니까 답변에 필요한 데이터를 넘겨야 하는거지"
  - "이건 새로운 스펙이 필요한거 같다. 스펙을 작성하자"

## 요약

- 기존 `docs/specs` 문서 형식을 확인했다.
- 신규 아키텍처 스펙 `docs/specs/v4/SPEC.md`를 추가했다.
- 구현 단위 PRD `docs/specs/v4/prds/phase21-skill-owned-llm.md`를 추가했다.

## 핵심 내용

- 스킬 경로에서 최종 응답 LLM 소유권을 프로젝트 내부 provider가 아니라 호출자 쪽으로 이동
- 프로젝트는 구조화된 분석 payload 또는 prompt-ready package 반환
- `payload / prompt-ready / legacy` 실행 모드 정의
- 1차 범위는 `daily / recommend / perspective` 같은 사용자-facing 명령으로 제한

## 메모

- 현재 저장소는 `config.yaml` 기반으로 프로젝트 내부에서 Codex/Anthropic를 직접 고르는 구조다.
- 이번 스펙은 그 구조를 즉시 제거하는 문서가 아니라, 단계적으로 분리하는 전환 설계다.
