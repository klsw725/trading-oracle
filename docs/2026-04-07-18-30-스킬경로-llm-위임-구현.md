# 작업 기록 — 스킬 경로 LLM 위임 구현

- 시각: 2026-04-07 18:30 (Asia/Seoul)
- 사용자 프롬프트:
  - "작업 진행"

## 요약

- 스킬 경로 전용 `--llm-mode payload|prompt-ready` 분기를 구현했다.
- 로컬 CLI/비스킬 경로는 기존 내부 provider 흐름을 유지하도록 두었다.

## 변경 파일

- `src/common.py`
- `scripts/daily.py`
- `scripts/recommend.py`
- `scripts/perspective.py`
- `README.md`
- `SKILL.md`

## 구현 내용

- `src/common.py`
  - 스킬 경로용 payload 조립 helper 추가
  - perspective prompt package 생성 helper 추가
  - `run_single_perspective()`에 `llm_mode` 분기 추가
  - `run_recommend()`에 `llm_mode` 분기 추가
  - `NumEncoder`에 `np.bool_` 직렬화 추가
- `scripts/daily.py`
  - `--llm-mode` 플래그 추가
  - payload/prompt-ready JSON 출력 분기 추가
- `scripts/recommend.py`
  - `--llm-mode` 플래그 추가
  - payload/prompt-ready 결과를 JSON으로 직접 출력
- `scripts/perspective.py`
  - `--llm-mode` 플래그 추가
  - payload/prompt-ready 결과를 직접 출력
- 문서
  - README / SKILL에 스킬 경로용 호출 예시 추가

## 검증

- `uv run python -m py_compile scripts/daily.py scripts/recommend.py scripts/perspective.py src/common.py`
- `uv run scripts/perspective.py --quant -t 005930 --json --llm-mode payload`
- `uv run scripts/perspective.py --macro -t 005930 --json --llm-mode prompt-ready`
- `uv run scripts/recommend.py --top 1 --json --llm-mode payload`
- `uv run scripts/daily.py --json --llm-mode payload`

## 메모

- LSP error-level diagnostics는 `src/common.py`에 다수 존재했지만 대부분 기존 타입 주석 부족/pyright 설정 성격의 선행 이슈로 보였다.
- 이번 작업에서는 스킬 경로 분기와 실제 payload/prompt-ready 출력 동작을 런타임 커맨드로 확인했다.
