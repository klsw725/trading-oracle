# Phase 7: 일간 변동 리포트 (Daily Delta Report)

## 프롬프트
```
D Next → Phase 7 일간 변동 리포트 추천 → gogogo
```

## 변경 사항

### 새 파일
- `docs/specs/multi-perspective/prds/phase7-daily-delta.md` — PRD

### 수정 파일
- `src/performance/tracker.py` — `compute_delta()` 추가
- `scripts/daily.py` — delta 계산 + `_print_delta()` 터미널 출력 + JSON delta 필드
- `main.py` — delta 계산 + JSON delta 필드
- `docs/specs/multi-perspective/SPEC.md` — Phase 7 PRD 연결

## 설계 결정

1. **반환 구조 변경 없음** — `run_multi_perspective()` 반환값 유지, delta는 호출자에서 별도 계산
2. **graceful degradation** — 이전 스냅샷 없으면 delta 섹션 생략
3. **관점별 변동 추적** — 합의 verdict뿐 아니라 어떤 관점이 바뀌었는지도 표시

## 검증
- 동일 데이터 비교 → 변동 없음 ✅
- SELL→HOLD 변경 → 감지 + 관점별 변동 추적 ✅
- 새 종목/제거 종목 → 감지 ✅
